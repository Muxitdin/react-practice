import React from "react";
import logo from "./../../images/logo.png"
import './style.css';

const Header = () => {
    return (
        <header className="header">
            <img src={logo} alt="#" />
        </header>
    );
}

export default Header;